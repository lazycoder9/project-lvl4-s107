extends /layouts/application.pug

block content
  +formFor(f, { url: urlFor('tasks'), method: 'post' })
    +input(f, 'name')
    +input(f, 'description')
    +select(f, 'assignedTo', users)
    +button(f)
